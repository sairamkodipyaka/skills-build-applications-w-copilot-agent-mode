# OctoFit Tracker — Backend

Run locally:

```bash
cd octofit-tracker/backend
npm install
npm run dev
```

Backend server will default to port 8000 and connects to MongoDB at `mongodb://localhost:27017/octofit` (MongoDB uses port 27017).
